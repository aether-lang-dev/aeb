# The Aether release aeb's OWN machinery is built against.
#
# This is NOT the Aether that compiles your program — that one is yours,
# resolved from $AETHER / PATH, and aeb never touches it. This pin is only
# for compiling `.foo.ae` build files into aeb's orchestrator, and is an
# implementation detail of this aeb release (like the gcc that built your
# `make` binary).
#
# aeb fetches this version into its own cache on first use if the ae on
# PATH is older. See asks/two-aethers-pinned-toolchain-vs-declared-dep.md
#
# One bare version string. Bump when aeb starts calling a primitive that
# an older Aether lacks (e.g. os.spawn_proc needed 0.442,
# os.wait_any_timeout needed 0.447, string.replace_all needed 0.463).
#
# This is a FLOOR, not a preference: it is the oldest Aether that can
# compile aeb's own sources. Anything newer on PATH is accepted as-is.
# Keep it at the real requirement — raising it to chase a nicer release
# forces a needless fetch on everyone sitting between the two versions.
#
# SEPARATE CONCERN — glibc. Releases up to ~0.449 were built on
# ubuntu-latest and carry a GLIBC_2.38 floor, so `aetherc` dies on Debian
# 12 (glibc 2.36) while `ae --version` still works. Aether's release.yml
# now pins ubuntu-22.04 (glibc 2.35) and 0.452.0 measures at GLIBC_2.34.
# That affects WHICH RELEASE TO FETCH when the pin is not satisfied, not
# what the floor is — so it belongs in the fetch logic, not here.
#
# Raised 0.541.0 -> 0.593.0 (2026-08-28) with the DSL rework release. The
# rework's core mechanism — the bldr.build() wrapper + all 147 builders
# reading builder_context(), plus the ctx→_ctx injection across the graph
# verbs — is what the whole b-free grammar rests on, and it was authored and
# validated end-to-end against ae 0.587-0.593 on Linux + the cachyos box.
# We deliberately keep NO back-compat for the pre-bldr (build.*) grammar, so
# the floor moves up with FETCH rather than staying a sparse historical
# minimum: an aeb built from this tree only makes sense on the Aether the
# rework was built against.
#
# Raised 0.593.0 -> 0.599.0 (2026-08-29) in lockstep with AETHER_FETCH for the
# per-platform bundle release: 0.599.0 is the first published aether that ships
# libaether.h in the release tarball + exposes the linux-musl cross targets.
#
# Raised 0.599.0 -> 0.602.0 (2026-08-29) in lockstep for the FreeBSD bundle:
# 0.602.0 publishes the lean FreeBSD cross sysroot as a release asset the
# release.yml bundle step fetches (keyed to FETCH). See AETHER_FETCH for detail.
#
# Raised 0.602.0 -> 0.609.0 (2026-08-30) in lockstep with AETHER_FETCH ahead of
# the v0.289 release. aeb's full suite (133/133) builds + passes against the real
# 0.609.0 linux tarball; glibc floor is GLIBC_2.34 (Debian-12 safe). Bundles the
# accumulated 0.603-0.609 fixes; nothing here calls a new primitive, but the
# no-back-compat DSL policy keeps PIN moving with FETCH.
#
# Raised 0.609.0 -> 0.613.0 (2026-08-31) in lockstep with AETHER_FETCH for the
# std.spec run_summary contract change. As of 0.612.0, spec.run_summary RETURNS
# its verdict (0 all-green / 1 failed) instead of exit(1)-on-failure; a bare
# `spec.run_summary(fw)` last statement now silently turns a FAILING suite green
# (main()'s return value is the process exit status, and a dropped verdict exits
# 0). All 132 aeb tests were swept from the bare form to `return spec.run_summary(fw)`
# and REQUIRE >= 0.612 to reflect failures. Verified against the real 0.613.0 tarball:
# pass->0, fail->1 (returned verdict reddens), bare->0 (the footgun) — and the full
# suite is green. 0.613 (the newest published) also carries the SIGPIPE server-death
# and sleep-signal fixes; glibc floor stays GLIBC_2.34. This is a genuine floor move
# (the returned-verdict semantics), not just the no-back-compat lockstep.
#
# Raised 0.613.0 -> 0.636.0 (2026-09-05) in lockstep with AETHER_FETCH. 0.636 is
# all diagnostic-QUALITY fixes (hand-built diagnostics now carry file/line/snippet
# via the active source context; carets point at the token START not one past it;
# "undefined function" underlines the callee not the paren) — no grammar/API/
# primitive change, so nothing here newly requires it; the floor moves with FETCH
# per the no-back-compat DSL policy. Bonus: aeb's own W1001 unused-variable build
# warnings now carry a location instead of a bare message. Verified against the
# real 0.636.0 linux tarball: aeb builds clean, full suite 134/134; glibc floor
# GLIBC_2.34 (Debian-12 safe); all platform assets published.
#
# Raised 0.636.0 -> 0.638.0 (2026-09-05) in lockstep with AETHER_FETCH. No
# grammar/API/primitive change aeb newly requires; the floor moves with FETCH per
# the no-back-compat DSL policy. Verified against the real 0.638.0 linux tarball:
# aeb builds clean, full suite 134/134; glibc floor GLIBC_2.34 (Debian-12 safe);
# all platform assets published.
#
# Raised 0.638.0 -> 0.640.0 (2026-09-05) in lockstep with AETHER_FETCH. 0.640's
# change is an `ae add <pkg>@<tag>` dependency-pin fix — aeb doesn't use `ae add`
# (it resolves JVM deps via its own bld-backed resolver), so no functional impact;
# the floor moves with FETCH per the no-back-compat DSL policy. Verified against
# the real 0.640.0 linux tarball: aeb builds clean, full suite 134/134; glibc floor
# GLIBC_2.34 (Debian-12 safe); all platform assets published.
#
# Raised 0.640.0 -> 0.643.0 (2026-09-06) in lockstep with AETHER_FETCH. 0.643 fixes
# a heap-string-tracker codegen bug that silently corrupted structs sharing a punned
# field prefix (no diagnostic) — a real memory-safety fix, though not one aeb newly
# requires; the floor moves with FETCH per the no-back-compat DSL policy. Verified
# against the real 0.643.0 linux tarball: aeb builds clean, full suite 134/134;
# glibc floor GLIBC_2.34 (Debian-12 safe); all platform assets published.
#
# Raised 0.643.0 -> 0.645.0 (2026-09-06) in lockstep with AETHER_FETCH. 0.645 adds
# a lint (warn on `malloc(<literal>) as *T`, the layout-corruption pattern 0.643
# fixed) — a purely additive diagnostic, no grammar/API/primitive change aeb needs;
# the floor moves with FETCH per the no-back-compat DSL policy. Verified against
# the real 0.645.0 linux tarball: aeb builds clean, full suite 134/134; glibc floor
# GLIBC_2.34 (Debian-12 safe); all platform assets published.
#
# Raised 0.645.0 -> 0.650.0 (2026-09-08) in lockstep with AETHER_FETCH. 0.650 is
# cache-handling fixes (std.mutation no longer rm -rf's the shared ~/.aether/cache
# mid-build; `ae cache` resolves the right dir) — no grammar/API/primitive change
# aeb needs; the floor moves with FETCH per the no-back-compat DSL policy. Verified
# against the real 0.650.0 linux tarball: aeb builds clean, full suite 134/134;
# glibc floor GLIBC_2.34 (Debian-12 safe); all platform assets published.
#
# Raised 0.650.0 -> 0.652.0 (2026-09-10) in lockstep with AETHER_FETCH. 0.651 +
# 0.652 are Windows-contrib (windows.h macro collision, utf8proc) + `ae cache`
# override fixes — no grammar/API/primitive change aeb needs; the floor moves
# with FETCH per the no-back-compat DSL policy. Verified against the real 0.652.0
# linux tarball: aeb builds clean, full suite 134/134; glibc floor GLIBC_2.34
# (Debian-12 safe); all platform assets published.
#
# Raised 0.652.0 -> 0.665.0 (2026-09-10) in lockstep with AETHER_FETCH. A
# 13-version jump; scanned 0.653-0.665 release notes — all additive (std.resp,
# std.cbor et al.) + fixes, NO grammar/API/removed/breaking change aeb touches;
# the floor moves with FETCH per the no-back-compat DSL policy. Verified against
# the real 0.665.0 linux tarball: aeb builds clean, full suite 134/134; glibc
# floor GLIBC_2.34 (Debian-12 safe); all platform assets published.
#
# Raised 0.665.0 -> 0.666.0 (2026-09-13). Newest published aether; all fixes +
# perf, no grammar/API/removed/breaking change (scanned the notes): tcp_receive/
# casper leak fixes, string.release-untracked fix, a long-build-path compiler-
# command-truncation fix (mildly relevant — aeb assembles long commands),
# foo()->foo(void) + -Wstrict-prototypes, and a 4.5x-faster type-check. Verified
# against the real 0.666.0 linux tarball: aeb builds clean, full suite 136/136,
# the fat aeb-resolve.jar builds + resolves; glibc floor GLIBC_2.34 (Debian-12
# safe). PIN moves in lockstep with FETCH (no-back-compat DSL policy).
#
# Raised 0.666.0 -> 0.668.0 (2026-09-14). This is a REAL floor move: aeb now
# imports std.zip (added in 0.667.0) in lib/bldr's _unzip_to_dir, so it cannot
# compile on an older Aether. 0.667 added std.zip (a buffer ZIP reader) + the
# E0200 setter-in-builder-position compile error; 0.668 is fixes. The SDK's
# `unzip`-shell-out sites (java shade/package_jar, scala/clojure assembly, fetch)
# now extract via std.zip through bldr._unzip_to_dir — dropping the `unzip` binary
# dependency. Verified against the real 0.668.0 linux tarball: aeb builds clean,
# full suite 136/136, _unzip_to_dir extracts byte-identical to `unzip -qq -o`, fat
# aeb-resolve.jar builds + resolves; glibc floor GLIBC_2.34 (Debian-12 safe).
#
# Raised 0.668.0 -> 0.670.0 (2026-09-14). A REAL floor move: aeb now calls the
# std "command-alike" primitives added in 0.670.0 (fs.copy_tree/remove_tree/
# make_temp_dir/make_temp_file/find_ext/find_ext_excluding, os.cpu_count,
# cryptography.sha256_file, string.lines) to replace its cp -R / rm -rf / mktemp
# / find / sha256sum / nproc shell-outs — so it cannot compile on an older
# Aether. (These are the primitives aeb's own census motivated; scaffolded here,
# merged upstream as PR #2017, shipped in 0.670.0.) Verified against the real
# 0.670.0 linux tarball: aeb builds clean, full suite green; glibc floor
# GLIBC_2.34 (Debian-12 safe). PIN moves in lockstep with FETCH.
#
# Raised 0.670.0 -> 0.672.0 (2026-09-15). A REAL floor move: aeb now calls
# string.version_compare (0.672.0, filevercmp/sort -V semantics, extension-aware)
# in bldr._newest_jar to pick the newest runtime jar off disk, replacing the
# clojure finders' `ls <globs> | grep -v sources | sort -Vr | head -1` shell-out
# — so it cannot compile on an older Aether. (aeb's own ask motivated the
# primitive; merged upstream as PR #2021, shipped in 0.672.0.) Verified against
# the real 0.672.0 linux tarball: aeb builds clean, full suite green; glibc floor
# GLIBC_2.34 (Debian-12 safe). PIN moves in lockstep with FETCH.
#
# Raised 0.672.0 -> 0.675.0 (2026-09-15). A real floor move: aeb now imports
# std.strarr (new/push_copy/get/size/array/free) in bldr._glob_sorted_desc to
# collect + version-sort the groovy runtime-jar and /usr/lib/jvm JDK finders
# newest-first, replacing their `ls <globs> | grep -v … | sort -Vr` shell-outs.
# std.strarr is 0.675-only (verified ABSENT in 0.673/0.674, PRESENT in 0.675 —
# PR #2034, which first shipped in 0.675.0, NOT 0.674.0 which predated its merge),
# so aeb genuinely cannot compile below 0.675. (The sort itself is an INLINE
# insertion sort over string.version_compare, NOT sort.strings_by(fn) — a
# comparator passed as a fn-VALUE from a separately-compiled library module is
# DCE'd, aether issue #2037; strarr/push_copy still do the collection.)
#
# HONESTY NOTE: unlike the DSL-rework (0.593) or returned-verdict (0.612) bumps,
# this floor move was ELECTIVE — the two finders worked fine via the shell
# `sort -Vr`; converting them (to finish the coreutils→stdlib sweep + drop the
# last MSYS deps on Windows) is what pulled the floor to a same-day release. Kept
# deliberately (aeb has no downstream community yet; FETCH policy says near-HEAD
# is cheap for now). If a consumer community forms, weigh reverting these two
# finders to the shell to lower the floor, per the "don't chase a nicer release"
# rule at the top of this file. Verified against the real 0.675.0 linux tarball:
# aeb builds clean, full suite 136/136; GLIBC floor 2.34. PIN lockstep with FETCH.
#
# Raised 0.675.0 -> 0.677.0 (2026-09-16). A REAL floor move for DOWNSTREAM
# consumers: 0.677 carries the codegen fix that emits @c_callback definitions
# WEAK (aether 7ed91d46), so two translation units that each emit the same
# callback share one definition instead of colliding at link. aeb's SDK closures
# (fs.walk / seq_each predicates — the source-discovery + staleness walks) lower
# to @c_callback, so a CONSUMER node that pulls two such TUs together fails to
# link on an older aether with a duplicate-symbol error. aeb itself and its warm
# self-suite don't hit it (single-TU); the cold consumer build does — the same
# class as the v0.311 E0200/seq_filter escape (now gated by
# itests/sdk-cold-compile-smoke.sh). Also folds 0.676 (spawn signal-mask reset
# before exec). Verified against the real 0.677.0 linux tarball: aeb builds
# clean, cold-compile gate green (6/6 SDKs), full suite 136/136; GLIBC floor 2.34
# (Debian-12 safe). PIN moves in lockstep with FETCH.
#
# Raised 0.677.0 -> 0.680.0 (2026-09-17). PIN moves in lockstep with FETCH; the
# floor aeb's OWN sources require is unchanged (still std.strarr @0.675 + the
# @c_callback-weak codegen @0.677 for downstream consumers). The reason to prefer
# 0.680 is a RELEASE-BUILD fix: 0.679's FreeBSD `--emit=lib` link added -shared
# -fPIC only on the final link line, but the runtime/stdlib objects in
# libaether.a were still compiled without -fPIC, so a FreeBSD-15 `--emit=lib`
# target failed with R_X86_64_* relocation errors (+ a tls_depth TLS reloc).
# 0.680 compiles those objects with -fPIC on every ELF/PE --emit=lib/staticlib
# target and fixes the TLS model. aeb's release.yml cross-builds the FreeBSD
# bundle leg, so this is what keeps that asset linkable. Verified against the
# real 0.680.0 linux tarball: aeb builds clean, cold-compile gate green, override
# -dep smoke 6/6, full suite 149/149; GLIBC floor 2.34 (Debian-12 safe).
#
# Raised 0.680.0 -> 0.681.0 (2026-09-17). A REAL floor move: bldr._host_arch()
# now calls os.arch() (aether #2051, first in 0.681) instead of shelling
# `uname -m`. os.arch() is a compile-time CPU-arch primitive returning aeb's
# exact ecosystem vocabulary (x86_64/arm64/…, never amd64/aarch64) — no process
# spawn (works with no shell / in a sandbox) AND correct under cross-compile
# (reports the TARGET arch, where uname -m gives the build host's). The last
# coreutils shell-out in the arch path is gone. Verified against the real 0.681.0
# linux tarball: aeb builds clean, cold-gate + override-dep smoke + full suite
# green; GLIBC floor 2.34 (Debian-12 safe). PIN moves in lockstep with FETCH.
#
# Raised 0.681.0 -> 0.686.0 (2026-09-18). A FETCH-side lockstep move, not a real
# floor: aeb calls no new 0.682-0.686 primitive, so its own sources still compile
# on 0.681 — the floor moves with FETCH per the no-back-compat DSL policy. Scanned
# 0.682-0.686; all fixes + tests, no grammar/API/removed change aeb touches. Two
# are worth naming. (1) 0.683 fixes the E0200 "block setter called as a node
# builder" discriminator (0.667's rule mis-rejected DSL containers in modules that
# ALSO declare a builder — aether-ui#147); aeb relies on that same
# setter-in-builder-position rule (itests/setter-mangle-collision.sh), and 0.683's
# fix keeps aeb's void-setter misuse still caught while unbreaking handle-returning
# containers — so it's aeb-adjacent even though aeb didn't hit the false-reject.
# (2) 0.686 adds the std.snapshot concurrent-CAS test (500 concurrent CAS land
# exactly 500; 300 writers vs 300 readers, no torn reads) — motivated directly by
# selaenium's Grid hub registry, the pool-owned COW work this session helped land
# (the send-path/reclamation arc, aether #2082/#2083). A pleasant bookend, not an
# aeb requirement. Also folds 0.682 (float const-fold spelling + Windows crash
# names), 0.684 (mixed-string-return closure UAF — same closure-return class as the
# #2054/PR #2034 family aeb has been bitten by, now belt-and-suspenders), 0.685
# (fs.realpath Windows UNC + long/int const-fold). Verified against the real
# 0.686.0 linux tarball: aeb builds clean, cold-gate + override-dep smoke + full
# suite green; GLIBC floor 2.34 (Debian-12 safe). PIN moves in lockstep with FETCH.
#
# Raised 0.686.0 -> 0.695.0 (2026-09-19). FETCH-side lockstep, and this one is
# ECOSYSTEM-RELEVANT to a feature aeb now ships: aeb's aether SDK gained
# aether.emit_binary_package (this release), which produces `ae add binary`-
# consumable packages — the PRODUCER side of aether/tools/ae.c ae_try_binary_
# package (0.694). 0.694 added `ae add` binary-package INSTALL; 0.695 makes the
# CONSUMER robust — a binary-package `import` inside a transitively-imported /
# dotted wrapper module now resolves (the prepass walks the full source-import
# graph), not just an import written in the entry file. Together they complete
# the round trip aeb's emitter targets (asked for by the libphonenumber-ae /
# datastar line). aeb calls no new primitive from 0.687-0.695 — its own sources
# still compile on 0.681 — so the floor still moves with FETCH, not because aeb
# requires it; but pinning 0.695 means an aeb that emits ae-add packages travels
# with the ae that best consumes them. Scanned 0.687-0.695: 0.687 operator-chain
# >256 nesting fix, 0.688 Windows fs.symlink + readlink leak, 0.689 nested-interp-
# in-println crash, 0.691 tuple-into-scalar-param now a compile error (aeb builds
# clean — no latent tuple-arg bug), 0.692 Windows test-cleanup + 0.693 LSP/UTF-8,
# 0.694 `ae add` binary install, 0.695 the consumer import-graph walk. Verified
# against the real 0.695.0 linux tarball: aeb builds clean, cold-gate + override-
# dep smoke + ae-add-binary-package smoke (6/6, incl. real `ae add` round trip) +
# full suite 151/151 green; GLIBC floor 2.34. PIN moves in lockstep with FETCH.
#
# Raised 0.695.0 -> 0.696.0 (2026-09-19). FETCH-side lockstep, and ecosystem-
# relevant to aeb's aether.emit_binary_package: 0.696 HARDENS the `ae add` binary
# contract this SDK produces — a binary package now REQUIRES a published .sha256
# (was: warn + install unverified). aeb's emitter already writes the sidecar
# unconditionally, so aeb-emitted packages satisfy the stricter rule (verified:
# `ae add` on 0.696 installs an aeb-emitted package "Checksum verified"). 0.696
# also adds `ae add --target <triple>` (fetch a FOREIGN platform's binary package
# for cross-platform bundling) — the consume-side complement to emit_binary_
# package's target() cross-emit, closing the cross-platform loop. aeb calls no new
# primitive from 0.696, so its own sources still compile on 0.681 (the real floor
# aeb-link now enforces on the linked libaether.a); the pin moves with FETCH.
# Verified against the real 0.696.0 linux tarball: aeb builds clean, cold-gate +
# override-dep + ae-add + libaether-floor-guard smokes + full suite green; GLIBC
# floor 2.34. PIN moves in lockstep with FETCH.
0.696.0
