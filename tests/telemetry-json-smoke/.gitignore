# Generated by run-smoke.sh — the build tree and the JSON outputs it
# asserts on. Only the fixture sources (.build.ae / .tests.ae / .ae /
# src/*.c) and run-smoke.sh are tracked.
/target/
*/target/
/smoke-out/
