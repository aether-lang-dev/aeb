# WHICH Aether release aeb downloads when AETHER_PIN is not satisfied.
#
# Separate from AETHER_PIN on purpose. The PIN is a FLOOR — the oldest
# Aether that can compile aeb's sources. This is a KNOWN-GOOD release to
# actually fetch, and it can be newer for reasons unrelated to language
# features. Today that reason is glibc: releases built on ubuntu-latest
# carry a GLIBC_2.38 floor whose `aetherc` dies on Debian 12 (glibc 2.36)
# — while `ae --version` still succeeds, so a version-only probe reports
# a healthy toolchain that cannot build. Aether's release.yml now pins
# ubuntu-22.04 (glibc 2.35); 0.452.0 measures at GLIBC_2.34 and compiles
# on Debian 12.
#
# Keeping these separate means someone on 0.451 is NOT forced to re-fetch
# just because a nicer release exists — their 0.451 already clears the
# 0.447 floor.
#
# --- MAINTENANCE POLICY (2026-07-27) ---
#
# Aether currently cuts releases FAST — seven on 2026-07-26 alone. Chasing
# that with a hand-edited file would be a full-time tic, so DON'T. The two
# files move on deliberately different clocks:
#
#   AETHER_PIN    moves only when aeb starts calling a primitive an older
#                 Aether lacks. Historically THREE times (0.410, 0.413,
#                 0.442-ish) across hundreds of releases. Bump it in the
#                 same commit that introduces the call, never speculatively.
#
#   AETHER_FETCH  moves when a known-good release is worth preferring for
#                 reasons unrelated to language features — today glibc
#                 portability. Bump on EVIDENCE (a release that fixes a
#                 real portability problem), not on novelty. A newer number
#                 here is not automatically better; it is another thing to
#                 have tested.
#
# MUST BE >= AETHER_PIN. This names the release aeb DOWNLOADS when the pin
# is unsatisfied, so a value below the floor would fetch a toolchain that
# cannot compile aeb — the fetch would "succeed" and the build would then
# fail with E0301 in a generated file, which is exactly the confusion the
# pin exists to prevent. Raised from 0.452.0 to match the 0.463.0 floor
# (string.replace_all). Checked: 0.463.0 keeps the GLIBC_2.34 floor that
# made 0.452.0 the safe choice, and publishes all five platform assets.
#
# aeb has no downstream user community yet, so keeping close to HEAD is
# cheap and we can afford to move fast. When that changes, the honest
# move is to slow AETHER_FETCH down — pin it to a release that has been
# soaked, and let AETHER_PIN keep being the sparse, evidence-driven floor
# it already is. The mechanism does not need to change for that; only the
# discipline about when these numbers move does.
#
# Raised 0.541.0 -> 0.560.0 (2026-08-21) ahead of cutting the first aeb
# release in 53 commits: this whole batch (the polyglot SDK builders, the
# .all.ae dotted-root link fix, the JVM classpath fixes) was built and
# verified against Aether HEAD in the 0.556-0.562 range on both Linux and
# macOS. 0.560.0 is the newest PUBLISHED Aether release (HEAD's 0.562 is
# not tagged/published yet), and it ships all five platform assets
# (linux/macos-arm64/macos-x86_64/windows/freebsd) with the GLIBC_2.34
# floor. The PIN floor stays 0.541.0 — nothing here calls a primitive
# newer Aether added, so raising the floor would force a needless re-fetch
# on everyone between 0.541 and 0.560 (per the floor policy above).
#
# Raised 0.560.0 -> 0.593.0 (2026-08-28) for the DSL rework release. The whole
# b-free (bldr.build()) rework was built + validated against ae 0.587-0.593.
# This time AETHER_PIN moves in lockstep to 0.593.0 too (not the usual
# floor-stays-put): the rework has NO back-compat with the pre-bldr grammar,
# so the floor IS the fetch here. 0.593.0 is published with all platform
# assets and is the version the cachyos box compiled the whole sweep with.
#
# Raised 0.593.0 -> 0.599.0 (2026-08-29) for the per-platform bundle release.
# 0.599.0 is the first published aether that BOTH ships libaether.h in the
# release tarball (so a fetched release can cross-compile runtime-linking
# programs — aeb's own tools) AND exposes the *-linux-musl cross targets.
# Verified against the real 0.599.0 tarball: libaether.h present at
# include/aether/libaether.h, and --target=x86_64-linux-musl produces a
# statically-linked binary. This unblocks the release.yml bundle matrix
# (6 core + 2 musl = 8). PIN moves in lockstep per the no-back-compat policy.
#
# Raised 0.599.0 -> 0.602.0 (2026-08-29) to add the FreeBSD bundle (9th target).
# 0.602.0 publishes a lean ~22MB FreeBSD cross sysroot as a release asset
# (aether-<v>-freebsd-x86_64-sysroot.tar.xz) — the release.yml bundle step
# fetches it (keyed to THIS FETCH version) and sets AETHER_SYSROOT for the
# x86_64-freebsd row. Verified against the real 0.602.0 assets: the sysroot
# cross-links both hello.ae and aeb-main.ae to 'ELF ... for FreeBSD 15.0' from
# Linux. FreeBSD is amd64-only. (0.600/0.601 skipped for the pin: 0.600 was pure
# HTTP perf; 0.601's sysroot shipped but we're on the 0.602 re-cut.)
#
# Raised 0.602.0 -> 0.609.0 (2026-08-30) for the v0.289 release. 0.609.0 is the
# newest published aether (all 6 platform assets; GLIBC_2.34, Debian-12 safe).
# Verified: aeb's full suite is 133/133 against the real 0.609.0 tarball, and the
# bld-backed resolver + 9-target cross bundles build clean. PIN moves in lockstep
# (no-back-compat DSL policy).
#
# Raised 0.609.0 -> 0.613.0 (2026-08-31) for the std.spec run_summary contract.
# 0.612.0 is where run_summary RETURNS its verdict (0/1) instead of exit(1)-ing
# — the change aeb's 132-test sweep to `return spec.run_summary(fw)` depends on
# (a bare last-statement call now silently greens a failing suite). We fetch
# 0.613.0, the newest published, which is 0.612 + the SIGPIPE-server-death,
# sleep-signal and ae-version snprintf-truncation fixes (all pure fixes, no
# grammar/API break). Verified against the real 0.613.0 tarball: aeb rebuilds
# clean, the full suite is green, and the run_summary semantics check reddens a
# failing suite (fail->1) while a bare call does not (bare->0). All 6 platform
# assets present; GLIBC_2.34. PIN moves in lockstep (this one IS a real floor —
# the returned-verdict semantics require >= 0.612).
#
# Raised 0.613.0 -> 0.636.0 (2026-09-05). 0.636 is the newest published aether;
# its changes are all diagnostic-quality (hand-built diagnostics gain file/line/
# snippet from the active source context; caret points at token start; undefined-
# function underlines the callee) — no grammar/API break, strictly safer. Verified
# against the real 0.636.0 tarball: aeb builds clean, full suite 134/134; glibc
# floor GLIBC_2.34 (Debian-12 safe); all platform assets present. PIN moves in
# lockstep (no-back-compat DSL policy).
#
# Raised 0.636.0 -> 0.638.0 (2026-09-05). Newest published aether; no grammar/API
# break. Verified against the real 0.638.0 tarball: aeb builds clean, full suite
# 134/134; glibc floor GLIBC_2.34 (Debian-12 safe); all platform assets present.
# PIN moves in lockstep (no-back-compat DSL policy).
#
# Raised 0.638.0 -> 0.640.0 (2026-09-05). Newest published aether; the change is an
# `ae add @tag` dependency-pin fix, which aeb does not use. Verified against the
# real 0.640.0 tarball: aeb builds clean, full suite 134/134; glibc floor
# GLIBC_2.34 (Debian-12 safe); all platform assets present. PIN in lockstep.
#
# Raised 0.640.0 -> 0.643.0 (2026-09-06). Newest published aether; fixes a heap-
# string-tracker codegen bug that silently corrupted punned-prefix structs. Verified
# against the real 0.643.0 tarball: aeb builds clean, full suite 134/134; glibc
# floor GLIBC_2.34 (Debian-12 safe); all platform assets present. PIN in lockstep.
#
# Raised 0.643.0 -> 0.645.0 (2026-09-06). Newest published aether; adds a
# malloc-literal-cast lint, no grammar/API break. Verified against the real
# 0.645.0 tarball: aeb builds clean, full suite 134/134; glibc floor GLIBC_2.34
# (Debian-12 safe); all platform assets present. PIN in lockstep.
#
# Raised 0.645.0 -> 0.650.0 (2026-09-08). Newest published aether; cache-handling
# fixes (std.mutation shared-cache wipe, `ae cache` dir resolution), no grammar/API
# break. Verified against the real 0.650.0 tarball: aeb builds clean, full suite
# 134/134; glibc floor GLIBC_2.34 (Debian-12 safe); all platform assets present.
# PIN in lockstep.
#
# Raised 0.650.0 -> 0.652.0 (2026-09-10). Newest published aether; 0.651/0.652 are
# Windows-contrib + `ae cache` override fixes, no grammar/API break. Verified
# against the real 0.652.0 tarball: aeb builds clean, full suite 134/134; glibc
# floor GLIBC_2.34 (Debian-12 safe); all platform assets present. PIN in lockstep.
#
# Raised 0.652.0 -> 0.665.0 (2026-09-10). Newest published aether; a 13-version
# jump, all additive + fixes (0.653-0.665 notes scanned, no grammar/API break).
# Verified against the real 0.665.0 tarball: aeb builds clean, full suite
# 134/134; glibc floor GLIBC_2.34 (Debian-12 safe); all platform assets present.
# PIN in lockstep.
#
# Raised 0.665.0 -> 0.666.0 (2026-09-13). Newest published aether; all fixes +
# perf (memory-leak fixes, string.release-untracked, compiler-command-length
# handling, foo()->foo(void), 4.5x type-check), no grammar/API break. Verified
# against the real 0.666.0 tarball: aeb builds clean, full suite 136/136, fat
# aeb-resolve.jar builds + resolves; glibc floor GLIBC_2.34 (Debian-12 safe).
# PIN in lockstep.
#
# Raised 0.666.0 -> 0.668.0 (2026-09-14). A real floor move: aeb imports std.zip
# (added 0.667.0) in lib/bldr's _unzip_to_dir, replacing the SDK's `unzip`
# shell-outs. 0.667 added std.zip + the E0200 setter-in-builder-position compile
# error; 0.668 is fixes. Verified against the real 0.668.0 tarball: aeb builds
# clean, full suite 136/136, _unzip_to_dir byte-identical to `unzip -qq -o`, fat
# aeb-resolve.jar builds + resolves; glibc floor GLIBC_2.34 (Debian-12 safe).
#
# Raised 0.668.0 -> 0.670.0 (2026-09-14). Real floor move: aeb calls the 0.670.0
# std command-alike primitives (fs.copy_tree/remove_tree/make_temp_*/find_ext,
# os.cpu_count, sha256_file, string.lines) in place of its coreutil shell-outs.
# Verified against the real 0.670.0 tarball: aeb builds clean, full suite green;
# glibc floor GLIBC_2.34 (Debian-12 safe). PIN in lockstep.
#
# Raised 0.670.0 -> 0.672.0 (2026-09-15). Real floor move: aeb calls 0.672.0's
# string.version_compare in bldr._newest_jar to replace the clojure runtime-jar
# `sort -Vr | head -1` finders. (aeb ask -> PR #2021, shipped in 0.672.0.)
# Verified against the real 0.672.0 tarball: aeb builds clean, full suite green;
# glibc floor GLIBC_2.34 (Debian-12 safe). PIN in lockstep.
#
# Raised 0.672.0 -> 0.675.0 (2026-09-15). Real floor move: aeb calls 0.675.0's
# std.strarr + std.sort.strings_by + version_compare in bldr._glob_sorted_desc
# (groovy + JDK version-sort finders). #2034 first shipped in 0.675.0 (not 0.674).
# Verified against the real 0.675.0 tarball: builds clean, suite green; GLIBC 2.34.
#
# Raised 0.675.0 -> 0.677.0 (2026-09-16). Real floor move for DOWNSTREAM consumers:
# 0.677's @c_callback-weak-emit codegen fix (7ed91d46) lets a consumer node linking
# two TUs that each emit an aeb SDK closure callback (fs.walk/seq_each) share one
# definition instead of a duplicate-symbol link error. Verified against the real
# 0.677.0 tarball: aeb builds clean, cold-compile gate + full suite green; GLIBC 2.34.
#
# Raised 0.677.0 -> 0.680.0 (2026-09-17). FETCH-side reason (not a new aeb-called
# primitive): 0.680 fixes the FreeBSD `--emit=lib` -fPIC link (0.679 added -fPIC
# only at link, not on the libaether.a object compiles) that aeb's release.yml
# FreeBSD bundle leg needs. Verified against the real 0.680.0 tarball: aeb builds
# clean, cold-gate + override-dep smoke + full suite green; GLIBC 2.34.
#
# Raised 0.680.0 -> 0.681.0 (2026-09-17). Real floor move: bldr._host_arch() calls
# 0.681's os.arch() (aether #2051) instead of `uname -m`. Verified against the real
# 0.681.0 tarball: aeb builds clean, gates green; GLIBC 2.34.
0.681.0
