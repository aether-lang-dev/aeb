import bldr
import bldr (dep, load_third_party_deps)
import jest

main() {
    bldr.build() {
        dep("libs/shared/product/state/.build.ae")
        load_third_party_deps("../../../../angular.deps.ae")
        jest.test()
    }
}
