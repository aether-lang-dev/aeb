import bldr
import bldr (dep, load_third_party_deps)
import ts
import ts (skip_lib_check)

main() {
    bldr.build() {
        load_third_party_deps("../../../base.deps.ae")
        dep("libs/shared/jsxify/.build.ae")
        ts.tsc() {
            skip_lib_check()
        }
    }
}
