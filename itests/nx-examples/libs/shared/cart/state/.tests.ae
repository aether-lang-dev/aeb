import bldr
import bldr (dep, load_third_party_deps)
import jest

main() {
    b = bldr.start()
    dep(b, "libs/shared/cart/state/.build.ae")
    load_third_party_deps(b, "../../../../angular.deps.ae")
    jest.test(b)
}
