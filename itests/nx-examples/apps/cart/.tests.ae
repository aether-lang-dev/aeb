import bldr
import bldr (dep, load_third_party_deps)
import jest

main() {
    b = bldr.start()
    dep(b, "apps/cart/.build.ae")
    load_third_party_deps(b, "../../react.deps.ae")
    jest.test(b)
}
