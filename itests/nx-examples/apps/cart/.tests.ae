import bldr
import bldr (dep, load_third_party_deps)
import jest

main() {
    bldr.build() {
        dep("apps/cart/.build.ae")
        load_third_party_deps("../../react.deps.ae")
        jest.test()
    }
}
