import bldr
import bldr (dep, load_third_party_deps)
import angular

main() {
    b = bldr.start()
    dep(b, "apps/products/.build.ae")
    load_third_party_deps(b, "../../angular.deps.ae")
    angular.ng_build(b)
}
