import bldr
import python

main() {
    bldr.build() {
        dep("libs/python/setuptools/.setuptools.whl.ae")
        dep("libs/python/pytest/.pytest.whl.ae")
        python.install()
        python.lint()
        python.typecheck()
    }
}
