import bldr
import python

main() {
    b = bldr.start()
    bldr.dep(b, "libs/python/setuptools/.setuptools.whl.ae")
    bldr.dep(b, "libs/python/pytest/.pytest.whl.ae")
    python.install(b)
    python.lint(b)
    python.typecheck(b)
}
