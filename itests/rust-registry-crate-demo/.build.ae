import bldr
import rust
import rust (crate_name, edition)

main() {
    bldr.build() {
        dep("libs/serde_json/.serde_json.crate.ae")
        rust.cargo_project() {
            crate_name("rust-registry-crate-demo")
            edition("2021")
        }
    }
}
