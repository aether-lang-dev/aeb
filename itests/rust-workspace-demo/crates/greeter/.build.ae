import bldr
import rust
import rust (crate_name, edition, ws_dep)

main() {
    bldr.build() {
        dep(".build.ae")
        rust.cargo_crate() {
            crate_name("greeter")
            edition("2021")
            ws_dep("serde")
        }
    }
}
