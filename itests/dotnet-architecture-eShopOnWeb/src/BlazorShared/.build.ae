import bldr
import dotnet
import dotnet (root_namespace, assembly_name, nuget)

main() {
    b = bldr.start()
    dotnet.build_project(b) {
        root_namespace("BlazorShared")
        assembly_name("BlazorShared")
        nuget("BlazorInputFile")
        nuget("FluentValidation")
    }
}
