// Build the program first (bldr.dep on its .build.ae), then run the
// argv assertion in test_argv.sh (auto-discovered by bash.test).
import bldr
import bash
import bldr (dep)

main() {
    bldr.build() {
        dep("app/.build.ae")
        bash.test()
    }
}
