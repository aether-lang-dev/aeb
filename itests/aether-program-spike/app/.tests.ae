// Build the program first (build.dep on its .build.ae), then run the
// argv assertion in test_argv.sh (auto-discovered by bash.test).
import build
import bash
import build (dep)

main() {
    b = build.start()
    dep(b, "app/.build.ae")
    bash.test(b)
}
