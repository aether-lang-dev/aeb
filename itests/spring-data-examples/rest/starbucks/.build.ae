import bldr
import bldr (dep)
import maven (load_bom_file)
import java
import java (release, source_layout, enable_preview, generated_sources)

main() {
    bldr.build() {
        load_bom_file("../../spring-boot.bom.ae")
        dep("org.springframework.boot:spring-boot-starter-data-rest")
        dep("org.springframework.boot:spring-boot-starter-data-mongodb")
        dep("org.springframework.boot:spring-boot-restclient")
        dep("com.querydsl:querydsl-mongodb:5.1.0")
        dep("com.querydsl:querydsl-apt")
        dep("com.fasterxml.jackson.module:jackson-module-parameter-names")
        dep("org.springframework.batch:spring-batch-core")
        dep("org.thymeleaf:thymeleaf")
        dep("org.projectlombok:lombok")
        dep("org.springframework.data:spring-data-rest-hal-explorer")
        java.javac() {
            release("25")
            source_layout("maven idiomatic")
            enable_preview()
            parameters()
            generated_sources("target/rest/starbucks/generated-sources")
        }
    }
}
