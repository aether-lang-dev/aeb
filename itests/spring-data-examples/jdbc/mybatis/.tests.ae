import bldr
import bldr (dep)
import maven (load_bom_file)
import java
import java (release, source_layout, test_timeout, parameters)

main() {
    bldr.build() {
        dep("jdbc/mybatis/.build.ae")
        load_bom_file("../../spring-boot.bom.ae")
        dep("org.springframework.boot:spring-boot-starter-test")
        dep("org.springframework.boot:spring-boot-data-jdbc-test")
        dep("org.junit.platform:junit-platform-console")
        dep("org.mybatis.spring.boot:mybatis-spring-boot-starter-test:3.0.4")
        dep("org.springframework.boot:spring-boot-data-jdbc-test")
        java.javac_test() {
            release("25")
            source_layout("maven idiomatic")
            parameters()
        }
        java.junit5() {
            test_timeout("30")
        }
    }
}
